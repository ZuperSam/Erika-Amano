apiVersion: v1
kind: Service
metadata:
    name: "kmac-efca"
    labels:
        app: "kmac-efca"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kmac-efca"